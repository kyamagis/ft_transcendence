export enum Direction {
  Neutral = 0,
  Up = 1,
  Down = 2,
}

export enum ScreenManagement {
  PongHome,
  VSCpu,
  LadderMatch,
  Error,
  LetterOfInvitation,
  InvitationalMatch,
  ResultScreen,
}

export enum VsModeScreenFlow {
  MatchWaiting = 0,
  GameReady = 1,
  DuringAMatch = 2,
  ResultScreen = 3,
}
